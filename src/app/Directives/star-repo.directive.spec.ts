import { StarRepoDirective } from './star-repo.directive';

describe('StarRepoDirective', () => {
  it('should create an instance', () => {
    const directive = new StarRepoDirective();
    expect(directive).toBeTruthy();
  });
});
